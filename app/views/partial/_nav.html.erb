<nav class="navbar navbar-Cortadito" role="navigation">
  <div class="container-fluid">
    <div class="nav-header">
      <button class="navbar-toggle collapsed" data-target="#menu" data-toggle="collapse" type="button">
        <span class="sr-only">Toggle Navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <% if (request['controller']== 'events' && (request['action']=='index')) %>
          <% if request['action'] %>
            <% elsif request['action'] == 'edit' %>
              <%= link_to :back, class:'navbar-brand' do %>
                <span class="glyphicon glyphicon-menu-left">| Back</span>
              <% end %>
            <% else %>
              <%= link_to root_path, class:"navbar-brand" do %>
                <span class="glyphicon glyphicon-menu-left">| Back</span>
              <% end %>
          <% end %>
        <% end %>
      </button>
      <%= link_to 'FUERAMADURO', root_path, class:"navbar-brand" %>
    </div>

    <div id="menu" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
        <li>
          <%= link_to 'Add Event', new_event_path %>
        </li>
        <li class="dropdown">
          <a class="dropdown-toggle" href="#" data-toggle="dropdown" role="button" aria-expanded="false">
            <%= current_user.email %>
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu" role="menu">

          </ul>
        </li>

        <% else %>
          <!-- code -->
        <% end %>
      </ul>
    </div>

  </div>
</nav>

    <!-- #menu.collapse.navbar-collapse
      ul.nav.navbar-nav.navbar-right
        - if current_user
          li
            = link_to 'Add Event', new_event_path
          li.dropdown
            a.dropdown-toggle href="#" data-toggle="dropdown" role="button" aria-expanded="false"
              = current_user.email
              span.caret
            ul.dropdown-menu role="menu"
              li
                a href="#"
                  | My Events
              // Todo show all events that belong to a user
              li
                = link_to 'Profile', edit_user_registration_path
              li.divider
              li
                = link_to 'Log Out', destroy_user_session_path, method: :delete
        - else
          li
            = link_to 'Register', new_user_registration_path
          li
            = link_to 'Log In', new_user_session_path -->
